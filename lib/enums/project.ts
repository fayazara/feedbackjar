export enum Status {
  active = "active",
  archived = "archived",
}

export enum FeedbackType {
  bug = "Bug",
  feature = "Feature",
  idea = "Idea",
  other = "Other",
}

export enum FeedbackStatus {
  open = "Open",
  inProgress = "In progress",
  closed = "Closed",
}
